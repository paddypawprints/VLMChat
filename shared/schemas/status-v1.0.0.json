{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "⚠️  SECURITY CRITICAL - DO NOT MODIFY WITHOUT REVIEW. This schema validates incoming MQTT messages and protects against malformed or malicious data. Changes must be coordinated across all validation layers (server MQTT validator, device validator, API specs).",
  "$id": "status-v1.0.0.json",
  "title": "Device Status",
  "description": "Device status change notification",
  "type": "object",
  "required": ["device_id", "timestamp", "status"],
  "properties": {
    "device_id": {
      "type": "string",
      "description": "Unique device identifier"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC datetime when status changed"
    },
    "status": {
      "type": "string",
      "enum": ["online", "offline", "error", "connecting", "connected", "disconnected"],
      "description": "Device status"
    },
    "error": {
      "type": "string",
      "description": "Error message if status is error (optional)"
    }
  }
}
