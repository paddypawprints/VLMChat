{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "⚠️  SECURITY CRITICAL - DO NOT MODIFY WITHOUT REVIEW. This schema validates incoming MQTT messages and protects against malformed or malicious data. Changes must be coordinated across all validation layers (server MQTT validator, device validator, API specs).",
  "$id": "heartbeat-v1.0.0.json",
  "title": "Device Heartbeat",
  "description": "Periodic heartbeat signal from device to indicate it's alive",
  "type": "object",
  "required": ["device_id", "timestamp", "status"],
  "properties": {
    "device_id": {
      "type": "string",
      "description": "Unique device identifier"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC datetime when heartbeat was sent"
    },
    "status": {
      "type": "string",
      "enum": ["online", "busy", "idle"],
      "description": "Current device status"
    },
    "uptime": {
      "type": "number",
      "description": "Device uptime in seconds (optional)"
    }
  }
}
